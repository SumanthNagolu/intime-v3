/**
 * Contact Section Hooks
 *
 * State management hooks for Contact sections.
 * Each hook handles:
 * - Local state management
 * - Syncing from initial data
 * - Save operations via tRPC
 * - Edit mode toggling
 */

export { useBasicInfoSection } from './useBasicInfoSection'
export { useEmploymentSection } from './useEmploymentSection'
export { useCommunicationSection } from './useCommunicationSection'
export { useSocialSection } from './useSocialSection'
export { useCandidateSection } from './useCandidateSection'
export { useLeadSection } from './useLeadSection'
export { useAddressesSection } from './useAddressesSection'
