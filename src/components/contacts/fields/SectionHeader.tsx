/**
 * Re-export SectionHeader from accounts for use in contacts
 * This ensures consistent section headers across both entities
 */
export { SectionHeader, default } from '@/components/accounts/fields/SectionHeader'
