/**
 * Re-export UnifiedField from accounts for use in contacts
 * This ensures consistent field rendering across both entities
 */
export { UnifiedField, default } from '@/components/accounts/fields/UnifiedField'
